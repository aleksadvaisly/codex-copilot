# GitHub Copilot Native Provider Plan For Codex -- Review 1

| ID    | Status  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| REQ1  | PASS    | `codex-rs/model-provider-info/src/lib.rs:37`, `codex-rs/model-provider-info/src/lib.rs:384`, `codex-rs/model-provider-info/src/model_provider_info_tests.rs:182`, `cargo test -p codex-model-provider-info`                                                                                                                                                                                                                                                     |
| REQ2  | PASS    | Built-in metadata is provider-correct and now includes the required static IDE headers at `codex-rs/model-provider-info/src/lib.rs:307-346`; assertions at `codex-rs/model-provider-info/src/model_provider_info_tests.rs:189-215`; `cargo test -p codex-model-provider-info`                                                                                                                                                                                   |
| REQ3  | PARTIAL | Native GitHub device auth, persistence and CLI entry points exist at `codex-rs/login/src/copilot_auth.rs:166-233`, `codex-rs/login/src/copilot_storage.rs:13-57`, `codex-rs/cli/src/login.rs:177-191`; `cargo test -p codex-login`; dedicated Copilot-specific device-flow tests are still missing                                                                                                                                                              |
| REQ4  | PASS    | Native runtime bearer retrieval exists via `AuthManager::native_github_copilot_bearer_only(...)` and shared provider resolution at `codex-rs/login/src/copilot_auth.rs:236-245`, `codex-rs/login/src/provider_auth.rs:60-98`; base URL reuse is covered by `codex-rs/core/src/client.rs:662-671` and `codex-rs/core/src/client_tests.rs:185-225`; `cargo test -p codex-login`, `cargo test -p codex-core current_client_setup_uses_stored_copilot_api_base_url` |
| REQ5  | PASS    | Typed Copilot `/models` parser exists at `codex-rs/models-manager/src/copilot_models.rs:13-171`; translator test at `codex-rs/models-manager/src/copilot_models.rs:178-237`; `cargo test -p codex-models-manager`                                                                                                                                                                                                                                               |
| REQ6  | PASS    | Translation to `ModelInfo` is centralized in `codex-rs/models-manager/src/copilot_models.rs:66-171`; `cargo test -p codex-models-manager`                                                                                                                                                                                                                                                                                                                       |
| REQ7  | PASS    | Copilot-specific model refresh path and provider-aware cache file are implemented at `codex-rs/models-manager/src/manager.rs:446-565`, `codex-rs/models-manager/src/manager.rs:681-694`; cache split test at `codex-rs/models-manager/src/manager_tests.rs:878-891`; `cargo test -p codex-models-manager`                                                                                                                                                       |
| REQ8  | PARTIAL | Minimum request policy for standard HTTP Responses turns is wired by reusing shared Copilot provider resolution in `codex-rs/core/src/client.rs:662-671` and `codex-rs/cli/src/responses_cmd.rs:35`; built-in Copilot IDE headers live in `codex-rs/model-provider-info/src/lib.rs:317-340`; a live streamed Copilot text-turn test is still missing                                                                                                            |
| REQ9  | PARTIAL | Dedicated `copilot` CLI login and status UX exists at `codex-rs/cli/src/login.rs:177-191`, `codex-rs/cli/src/login.rs:352-411`, but TUI and broader provider-aware UX are still not updated                                                                                                                                                                                                                                                                     |
| REQ10 | PARTIAL | Current implementation status is tracked in `docs/work/GITHUB_COPILOT_NATIVE_TASKS.md`; targeted verification passed with `cargo test -p codex-model-provider-info`, `cargo test -p codex-login`, `cargo test -p codex-models-manager`, `cargo test -p codex-core`, `cargo test -p codex-cli`; end-to-end live Copilot turn validation and final docs are still pending                                                                                         |

Convergence score: `0.60` full PASS, `1.00` including PARTIAL items.

## Verification Output

- `just fmt`
- `cargo test -p codex-model-provider-info`
- `cargo test -p codex-login`
- `cargo test -p codex-models-manager`
- `cargo test -p codex-core current_client_setup_uses_stored_copilot_api_base_url`
- `cargo test -p codex-core`
- `cargo test -p codex-cli`

All commands above passed for this iteration.
